Compilation : langage compilé que l'on traduit en objet  
Par la suite cette compilation suit l'étape du liking (lien) avant de pouvoir être lu par la machine.  
La compilation est faite par un outil (cmake pour C++ par exemple) qui génère un fichier Makefile qui génèrera la compilation.  

# 1
<img width="777" height="560" alt="image" src="https://github.com/user-attachments/assets/d456eed8-8849-46d5-b9a2-a6e65b6d4886" />  

faire un script d'installation qui clône le repo dans /tmp, faire un build qui créer un exécutable dans /usr/bin, créer un fichier de configuration dans /etc/teeeworlds.cfg.
Par la suite on ajoutera un service systemd pour auto start.
